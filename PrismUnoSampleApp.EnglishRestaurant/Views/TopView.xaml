<UserControl
    x:Class="PrismUnoSampleApp.EnglishRestaurant.Views.TopView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PrismUnoSampleApp.EnglishRestaurant.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mvvm="using:Prism.Mvvm"
    mvvm:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <Grid>
        <Image Source="{x:Bind ViewModel.Picture.Value, Mode=OneWay}"
               HorizontalAlignment="Stretch"
               VerticalAlignment="Stretch"
               MinWidth="500"
               MinHeight="500" />
        <Button Content="画像を開く" 
                HorizontalAlignment="Center"
                VerticalAlignment="Center" 
                Padding="10"
                Command="{x:Bind ViewModel.TakePictureCommand}" />
    </Grid>
</UserControl>
